<%= simple_form_for(@task) do |f| %>
  <%= f.error_notification %>

  <%= f.input :content %>
  <%= f.input :status, collection: Task.statuses.keys, selected: Task.done %>

  <%= f.button :submit, class: 'btn btn-primary' %>
  <%= link_to 'Back', tasks_path, class: 'btn btn-default' %>
<% end %>
